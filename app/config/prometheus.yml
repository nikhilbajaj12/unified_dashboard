global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: unified_dash_app_metrics
    static_configs:
      - targets:
          - unified_dash-app:8001
    metrics_path: /metrics

  - job_name: temporal_metrics
    scheme: https
    tls_config:
      insecure_skip_verify: true
    static_configs:
      - targets: []
    metrics_path: /metrics

  - job_name: phoenix_metrics
    scheme: https
    tls_config:
      insecure_skip_verify: true
    static_configs:
      - targets: []
    metrics_path: /metrics
